
<%= form_for([post, @comment]) do |f| %>
	<%= render 'shared/error_messages', object: f.object %>
	<div class="comment">
		<%= f.text_area :comment, placeholder: "Your comment..." %>
	</div>
	<%= f.submit "Create", class: "btn btn-large btn-primary" %>

<% end %>